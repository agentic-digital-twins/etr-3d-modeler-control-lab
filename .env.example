# Local/manual process configuration. Copy to .env for local overrides.
# The browser never reads MODEL_API_UPSTREAM; Vite uses it only for its server-side proxy.

MODEL_API_HOST=127.0.0.1
MODEL_API_PORT=4230
MODEL_VIEWER_HOST=0.0.0.0
MODEL_VIEWER_PORT=4231
MODEL_API_UPSTREAM=http://127.0.0.1:4230
